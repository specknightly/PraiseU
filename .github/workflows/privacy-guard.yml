name: Source-only privacy guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  privacy-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify no WorkRecord runtime data is tracked
        shell: bash
        run: |
          bash Scripts/verify-source-only.sh
